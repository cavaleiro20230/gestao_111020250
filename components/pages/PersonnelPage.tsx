import React from 'react';
import HumanResourcesPage from './HumanResourcesPage';

// This component is likely a legacy/obsolete file, with its functionality moved to HumanResourcesPage.
// We render HumanResourcesPage here to maintain compatibility if it's still referenced somewhere.
const PersonnelPage: React.FC = () => {
  return <HumanResourcesPage />;
};

export default PersonnelPage;
